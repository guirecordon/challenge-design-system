import type { StoryObj, Meta } from '@storybook/react'
import { Button } from '@java-challenge/react'

export default {
  title: 'Button',
  component: Button,
} as Meta

export const Primary: StoryObj = {}
